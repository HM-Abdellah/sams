import {API,setCsrf,getCsrf} from './api.js';
import {state,setState} from './state.js';
import {ui,renderAll} from './ui.js';
import {setupSignature} from './signature.js';

let busy=false;let clickTimer=null;
function currentMonth(){return state.month||new Date().toISOString().slice(0,7);}
async function loadClass(){if(!state.classId)return;try{const [students,attendance]=await Promise.all([API.students(state.classId),API.attendance(state.classId,currentMonth())]);setState({students:students.students||[],attendance:attendance.attendance||[]});renderAll();await loadSignature();}catch(e){ui.toast(e.message||'Erreur de chargement.',true);}}
async function boot(){if(busy)return;busy=true;try{const session=await API.session();if(!session.authenticated){location.href='login.php';return;}setCsrf(session.csrf||getCsrf());const classes=await API.classes();const list=classes.classes||[];setState({user:session.user,csrf:session.csrf||getCsrf(),classes:list,classId:list[0]?.id??null,month:currentMonth()});const month=document.querySelector('#monthSelect');if(month)month.value=state.month;const current=document.querySelector('#currentUser');if(current)current.textContent=`${state.user.full_name} · ${state.user.role}`;if(state.user.role!=='admin')document.querySelectorAll('.admin-only').forEach(e=>e.remove());renderAll();if(state.classId)await loadClass();}catch(e){ui.toast(e.message||'Erreur de démarrage.',true);}finally{busy=false;}}
async function saveCell(td,forceAbsent=false){if(!state.classId||!td)return;const current=td.dataset.status||'';let status;if(forceAbsent)status='absent';else status=current===''?'present':current==='present'?'late':current==='late'?'excused':current==='excused'?'absent':'';const data={student_id:Number(td.dataset.student),attendance_date:td.dataset.date,period:Number(td.dataset.period)};try{if(!status)await API.deleteAttendance(data);else await API.setAttendance({...data,status});await loadClass();}catch(e){ui.toast(e.message||'Échec de sauvegarde.',true);}}
function wire(){
 document.querySelector('#reloadBtn')?.addEventListener('click',loadClass);document.querySelector('#logoutBtn')?.addEventListener('click',async()=>{try{await API.logout();location.href='login.php';}catch(e){ui.toast(e.message,true);}});
 document.querySelector('#themeBtn')?.addEventListener('click',()=>{document.body.classList.toggle('light');localStorage.setItem('sams-theme',document.body.classList.contains('light')?'light':'dark');});if(localStorage.getItem('sams-theme')==='light')document.body.classList.add('light');
 document.querySelector('#classSelect')?.addEventListener('change',async e=>{setState({classId:Number(e.target.value)});await loadClass();});document.querySelector('#monthSelect')?.addEventListener('change',async e=>{if(!/^\d{4}-\d{2}$/.test(e.target.value))return;setState({month:e.target.value});await loadClass();});
 document.querySelector('#studentSearch')?.addEventListener('input',e=>{setState({search:e.target.value});ui.attendance();});document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');setState({filter:btn.dataset.filter});ui.attendance();}));
 document.querySelectorAll('.tab').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b===btn));document.querySelectorAll('[data-panel]').forEach(p=>p.classList.toggle('hidden',p.dataset.panel!==btn.dataset.tab));setState({tab:btn.dataset.tab});if(btn.dataset.tab==='statistics')ui.statistics();}));
 const body=document.querySelector('#attendanceBody');body?.addEventListener('click',e=>{const td=e.target.closest('.attendance-cell');if(!td)return;clearTimeout(clickTimer);clickTimer=setTimeout(()=>saveCell(td),240);});body?.addEventListener('dblclick',e=>{const td=e.target.closest('.attendance-cell');if(!td)return;clearTimeout(clickTimer);saveCell(td,true);});body?.addEventListener('contextmenu',e=>{const td=e.target.closest('.attendance-cell');if(!td)return;e.preventDefault();saveCell(td,false).catch(()=>{});});
 document.querySelector('#addStudentBtn')?.addEventListener('click',()=>document.querySelector('#studentDialog').showModal());document.querySelector('#studentForm')?.addEventListener('submit',async e=>{e.preventDefault();const fn=document.querySelector('#firstNameInput').value.trim(),ln=document.querySelector('#lastNameInput').value.trim(),number=document.querySelector('#studentNumberInput').value.trim();try{await API.createStudent(state.classId,{first_name:fn,last_name:ln,student_number:number||null});e.target.closest('dialog').close();e.target.reset();await loadClass();ui.toast('Élève ajouté.');}catch(err){ui.toast(err.message,true);}});
 document.querySelector('#studentsList')?.addEventListener('click',async e=>{const btn=e.target.closest('[data-delete-student]');if(!btn)return;if(!confirm('Désactiver cet élève ?'))return;try{await API.deleteStudent(state.classId,Number(btn.dataset.deleteStudent));await loadClass();ui.toast('Élève désactivé.');}catch(err){ui.toast(err.message,true);}});
 document.querySelector('#addClassBtn')?.addEventListener('click',()=>document.querySelector('#classDialog').showModal());document.querySelector('#classForm')?.addEventListener('submit',async e=>{e.preventDefault();try{await API.createClass({name:document.querySelector('#classNameInput').value.trim(),level:document.querySelector('#classLevelInput').value.trim(),branch:document.querySelector('#classBranchInput').value.trim()});e.target.closest('dialog').close();e.target.reset();const c=await API.classes();setState({classes:c.classes||[]});ui.classes();ui.toast('Classe créée.');}catch(err){ui.toast(err.message,true);}});
 document.querySelector('#reportBtn')?.addEventListener('click',()=>document.querySelector('#reportDialog')?.showModal());document.querySelector('#officialReportBtn')?.addEventListener('click',e=>{e.preventDefault();document.querySelector('#reportDialog').close();window.print();});document.querySelector('#annualReportBtn')?.addEventListener('click',e=>{e.preventDefault();document.querySelector('#reportDialog').close();ui.statistics();document.querySelector('[data-tab="statistics"]').click();});
 setupSignature({canvas:document.querySelector('#signatureCanvas'),clearButton:document.querySelector('#clearSignatureBtn'),saveButton:document.querySelector('#saveSignatureBtn'),load:loadSignature});
}
async function loadSignature(){if(!state.classId)return;try{const d=await API.signature(state.classId);window.dispatchEvent(new CustomEvent('sams:signature-load',{detail:d.signature?.signature_data||''}));}catch(e){ui.toast(e.message,true);}}
window.addEventListener('DOMContentLoaded',()=>{wire();boot();});
